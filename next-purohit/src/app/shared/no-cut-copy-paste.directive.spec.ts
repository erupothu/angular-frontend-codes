import { NoCutCopyPasteDirective } from './no-cut-copy-paste.directive';

describe('NoCutCopyPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new NoCutCopyPasteDirective();
    expect(directive).toBeTruthy();
  });
});
